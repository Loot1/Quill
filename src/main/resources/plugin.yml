name: Quill
version: '${project.version}'
main: fr.loot1.quill.Quill
api-version: '1.19'
authors: [ Loot1 ]
commands:
  quill:
    description: Commande de gestion du plugin Quill
permissions:
  quill.*:
    description: Allows all permissions of Quill plugin
    default: op
    children:
      groom.reload: true
  quill.reload:
    description: Allows to reload plugin configuration file
    default: op
